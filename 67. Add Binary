class Solution:
    def addBinary(self, a: str, b: str) -> str:
        num1 = int(a, 2)
        num2 = int(b, 2)

        # Add the numbers
        total = num1 + num2

        # Convert the result back to binary string
        return bin(total)[2:]
        
